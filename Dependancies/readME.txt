Java uses API to establish a connection.

First, make them available to the curent class by typing the command,

`set classpath=mail.jar;activation.jar;.;`

Next carry on with your changes and then you should be able to run pretty fine.
